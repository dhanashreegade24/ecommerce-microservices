apiVersion: v1
kind: Service
metadata:
  name: productservice-service
spec:
  type: NodePort
  selector:
    app: productservice
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30008   # QA